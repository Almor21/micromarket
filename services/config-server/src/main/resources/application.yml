server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  config:
    import: optional:file:.env[.properties]
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations